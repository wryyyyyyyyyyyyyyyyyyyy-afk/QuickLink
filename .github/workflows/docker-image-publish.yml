name: Docker

on:
  push:
    tags:
      - '*'
  workflow_dispatch:


env:
  # TODO: Change variable to your image's name.
  IMAGE_NAME: image
  
jobs:
   docker-image-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
      
    steps:
      - name: Log into registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push image
        run: docker compose -f src/docker-compose.yml build && docker compose -f src/docker-compose.yml push
